apiVersion: v1
kind: Pod
metadata:
  name: hostname-pod
spec:
  containers:
  - name: container
    image: nginx
    volumeMounts:
    - name: host-volume
      mountPath: /usr/share/nginx/html
  volumes:
  - name: host-volume
    hostPath:
      path: /tmp/k8s-data
      type: DirectoryOrCreate
